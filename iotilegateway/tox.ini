[tox]
envlist = py

[testenv]
passenv=APPDATA
deps=
    pytest
    pytest-logging
    ../iotilecore
    ../iotiletest
    tornado
    futures
commands=
	py.test {posargs}

[pep8]
max-line-length = 240